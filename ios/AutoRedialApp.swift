// Minimal iOS SwiftUI client that calls the server API to start/stop jobs.
// Place this in an Xcode iOS app target.

import SwiftUI

@main
struct AutoRedialApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
